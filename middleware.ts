import {NextRequest,NextResponse} from 'next/server'
import {jwtVerify} from 'jose'
function resolveAuthSecret(){const s=process.env.AUTH_SECRET;if(s&&s.length>=32)return s;if(process.env.NODE_ENV==='production')throw new Error('AUTH_SECRET غير مضبوط أو أقصر من 32 حرف. حدد قيمة عشوائية قوية (32+ حرف) في متغيرات البيئة قبل تشغيل production.');return s||'dev-only-insecure-secret-change-me-before-prod'}
const secret=new TextEncoder().encode(resolveAuthSecret())
export async function middleware(req:NextRequest){const path=req.nextUrl.pathname;if(path.startsWith('/api/auth/')||path==='/api/certificates/verify'||path==='/login'||path==='/verify'||path.startsWith('/_next')||path==='/favicon.ico'||path==='/api/health')return NextResponse.next();const token=req.cookies.get('session')?.value;if(!token){if(path.startsWith('/api/'))return NextResponse.json({error:'unauthorized'},{status:401});return NextResponse.redirect(new URL('/login',req.url))}try{await jwtVerify(token,secret);return NextResponse.next()}catch{if(path.startsWith('/api/'))return NextResponse.json({error:'unauthorized'},{status:401});return NextResponse.redirect(new URL('/login',req.url))}}
export const config={matcher:['/dashboard/:path*','/students/:path*','/teachers/:path*','/courses/:path*','/groups/:path*','/attendance/:path*','/finance/:path*','/certificates/:path*','/reports/:path*','/ai/:path*','/users/:path*','/plans/:path*','/settings/:path*','/api/:path*']}
