'use client'
import {useState} from 'react'; import {Plus,Search,Edit3,Trash2} from 'lucide-react';
export default function DataTable({title,columns,initialRows,onAdd}:{title:string,columns:string[],initialRows:string[][],onAdd?:()=>void}){const [rows,setRows]=useState(initialRows);const [q,setQ]=useState('');return <div className="card"><div className="toolbar"><h2 style={{margin:0}}>{title}</h2><span className="grow"/><div className="search" style={{width:210}}><Search size={14}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="بحث..."/></div>{onAdd&&<button className="btn" onClick={onAdd}><Plus size={14}/> إضافة</button>}</div><div className="tableWrap"><table className="table"><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}<th>إجراء</th></tr></thead><tbody>{rows.filter(r=>r.join(' ').includes(q)).map((r,i)=><tr key={i}>{r.map((x,j)=><td key={j}>{j===r.length-1?<span className={'badge '+(x==='متأخر'?'yellow':x==='غير نشط'?'red':'')}>{x}</span>:x}</td>)}<td><button className="iconBtn" onClick={()=>setRows(rows.filter((_,n)=>n!==i))}><Trash2 size={14}/></button></td></tr>)}</tbody></table></div></div>}
