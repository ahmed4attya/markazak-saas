'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {ShieldCheck,Brain,BarChart3,LockKeyhole} from 'lucide-react';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('admin@center.sa');const [password,setPassword]=useState('admin123');const [err,setErr]=useState('');const [loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);setErr('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const j=await x.json();if(x.ok)r.push('/dashboard');else setErr(j.error||'خطأ');setLoading(false)}return <main className="loginPage"><div className="loginVisual"><div className="orb"/><div className="visualInner"><div className="brand big"><span>م</span></div><h1>إدارة مركز التدريب<br/>من مكان واحد</h1><p>الطلاب، الدورات، الجداول، الحضور، المالية والشهادات.</p><div className="visualCards"><div><BarChart3/><b>لوحة تحكم ذكية</b><small>مؤشرات التشغيل لحظة بلحظة</small></div><div><Brain/><b>مساعد AI</b><small>تحليل وتقارير وقرارات</small></div></div></div></div><div className="loginBox"><div className="loginBrand"><span>م</span><div><b>مركزك</b><small>Training Center OS</small></div></div><h2>تسجيل الدخول</h2><p className="muted">أدخل بيانات حساب مركزك</p><form onSubmit={submit}><label>البريد الإلكتروني</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label>كلمة المرور</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/>{err&&<div className="error">{err}</div>}<button className="primary full" disabled={loading}>{loading?'جارٍ الدخول...':'دخول إلى النظام'}</button></form><div className="secure"><LockKeyhole size={15}/> اتصال آمن ومشفر</div><div className="demo"><b>حساب التجربة</b><br/>admin@center.sa / admin123</div></div></main>}
